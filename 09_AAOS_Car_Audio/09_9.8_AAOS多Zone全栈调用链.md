## 9.8 AAOS多Zone全栈调用链

> [← 上一个](09_9.7_Audio_Mirroring-音频镜像.md) | [返回目录](README.md) | [下一个 →](09_9.9_CarAudioZoneConfig-Zone配置管理.md)

---

### 9.8.1 全栈调用链概述

AAOS多Zone音频系统涉及从App层到HAL层的完整调用链路。本章将深入解析三条核心调用链：**音量调节链路**、**焦点请求链路**、**设备切换链路**，从方法调用级别追踪每一步数据流向。

### 9.8.2 音量调节全栈调用链

#### 9.8.2.1 音量调节时序图

```mermaid
sequenceDiagram
    participant CarApp as Car App
    participant CarAM as CarAudioManager
    participant CarSvc as CarAudioService
    participant CZ as CarAudioZone
    participant CZC as CarAudioZoneConfig
    participant CVG as CarVolumeGroup
    participant AM as AudioManager
    participant AF as AudioFlinger
    participant HAL as AudioControl HAL
    CarApp->>CarAM: setGroupVolume(zoneId=0, groupId=0, index=50, flags)
    CarAM->>CarSvc: setGroupVolume() [Binder IPC]
    CarSvc->>CarSvc: enforcePermission(CAR_AUDIO_PERMISSION)
    CarSvc->>CZ: getVolumeGroup(groupId=0)
    CZ->>CZC: getCurrentCarAudioZoneConfig()
    CZC-->>CarSvc: CarVolumeGroup[0]
    CarSvc->>CVG: setVolumeIndex(index=50, flags)
    CVG->>CVG: 计算Gain值: minGain + index * stepValue
    CVG->>CVG: 检查Blocked/OverLimit/Attenuated状态
    CVG->>CVG: setCurrentGainIndexLocked(index)
    CVG->>AM: setAudioPortGain(portId, gainConfig)
    AM->>AF: AudioFlinger.setAudioPortConfig()
    AF->>HAL: setPortConfig() [HIDL/AIDL]
    HAL-->>AF: status=OK
    AF-->>CVG: setGain成功
    CVG->>CarSvc: onVolumeGroupChanged回调
    CarSvc->>CarSvc: 通知CarVolumeGroupEvent回调
```

#### 9.8.2.2 音量调节关键方法解析

**CarAudioService.setGroupVolume()**

```java
// CarAudioService.java
@Override
public void setGroupVolume(int zoneId, int groupId, int index, int flags) {
    // 1. 权限检查
    enforcePermission(Car.PERMISSION_CAR_AUDIO_VOLUME);
    // 2. 获取目标Zone的VolumeGroup
    CarVolumeGroup group = getCarVolumeGroup(zoneId, groupId);
    // 3. 检查音量是否被Blocked
    if (group.isBlocked()) {
        Slogf.w(TAG, "Volume group " + groupId + " is blocked");
        return;
    }
    // 4. 设置音量索引
    group.setVolumeIndex(index, flags);
    // 5. 持久化到Settings
    saveVolumeGroupSettingsLocked(zoneId, groupId, index);
}
```

**CarVolumeGroup.setVolumeIndex() Gain计算核心**

```java
// CarVolumeGroup.java
void setVolumeIndex(int index, int flags) {
    synchronized (mLock) {
        // 1. 范围限制
        index = Math.max(0, Math.min(index, getMaxGainIndex()));
        // 2. 计算Gain (millibels)
        int gainInMillibels = getMinGain() + index * getStepValue();
        // 3. 四层状态机检查
        if (isBlockedLocked()) return;          // Blocked优先级最高
        if (isOverLimitLocked()) gainInMillibels = mOverLimitGain; // OverLimit覆盖
        if (isAttenuatedLocked()) gainInMillibels = mAttenuatedGain; // Attenuated覆盖
        // 4. 设置到HAL
        mCurrentGainIndex = index;
        setDeviceGainGainLocked(gainInMillibels);
        // 5. 事件通知
        notifyVolumeGroupChangeLocked(flags);
    }
}
```

**Gain四层状态机优先级：**

```mermaid
flowchart TB
    INPUT[输入index] --> BLOCKED{isBlocked?}
    BLOCKED -->|Yes| REJECT[拒绝音量设置]
    BLOCKED -->|No| OVERLIMIT{isOverLimit?}
    OVERLIMIT -->|Yes| OLGAIN[使用OverLimit Gain]
    OVERLIMIT -->|No| ATTEN{isAttenuated?}
    ATTEN -->|Yes| ATTGAIN[使用Attenuated Gain]
    ATTEN -->|No| NORMAL[使用计算后的Normal Gain]
```

### 9.8.3 焦点请求全栈调用链

#### 9.8.3.1 焦点请求时序图

```mermaid
sequenceDiagram
    participant App as Audio App
    participant AM as AudioManager
    participant MFC as MediaFocusControl
    participant CZAF as CarZonesAudioFocus
    participant CAF as CarAudioFocus
    participant FI as FocusInteraction
    participant ACW as AudioControlWrapper
    participant HAL as AudioControl HAL
    App->>AM: requestAudioFocus(GAIN, attributes)
    AM->>MFC: requestAudioFocus() [内部调用]
    MFC->>MFC: 检测外部AudioPolicy存在
    MFC->>CZAF: onAudioFocusRequest(afi)
    CZAF->>CZAF: getAudioZoneIdForAudioFocusInfo(afi)
    Note over CZAF: 优先Bundle中ZONE_ID,否则UID映射
    CZAF->>CAF: mFocusHandlersForZones[zoneId].onAudioFocusRequest
    CAF->>FI: evaluateAgainstFocusHoldersLocked(afi)
    FI->>FI: 查询13x13交互矩阵
    FI-->>CAF: INTERACTION_CONCURRENT/EXCLUSIVE/MUTE
    CAF->>CAF: 根据交互类型决定焦点结果
    CAF->>ACW: onAudioFocusChange(zone, focusGain/Loss)
    ACW->>HAL: IAudioControl.onAudioFocusChange() [AIDL]
    CAF-->>MFC: setFocusRequestResult(GRANTED/DENIED)
    MFC-->>App: AUDIOFOCUS_REQUEST_GRANTED/DENIED
```

#### 9.8.3.2 Zone ID确定逻辑

```java
// CarZonesAudioFocus.java:211
public void onAudioFocusRequest(AudioFocusInfo afi) {
    // 确定焦点请求所属Zone
    int zoneId = getAudioZoneIdForAudioFocusInfo(afi);
    CarAudioFocus focusHandler = mFocusHandlersForZones.get(zoneId);
    if (focusHandler != null) {
        focusHandler.onAudioFocusRequest(afi);
    }
}

// Zone ID确定优先级
private int getAudioZoneIdForAudioFocusInfo(AudioFocusInfo afi) {
    // 1. 优先从Bundle中获取显式Zone ID
    Bundle bundle = afi.getAttributes().getBundle();
    if (bundle != null && bundle.containsKey(
            CarAudioManager.AUDIOFOCUS_EXTRA_REQUEST_ZONE_ID)) {
        return bundle.getInt(CarAudioManager.AUDIOFOCUS_EXTRA_REQUEST_ZONE_ID);
    }
    // 2. 通过UID到Zone的映射确定
    return mCarAudioService.getZoneIdForUid(afi.getUid());
}
```

#### 9.8.3.3 交互矩阵评估

```java
// FocusInteraction.java — 13x13交互矩阵
int evaluateAgainstFocusHoldersLocked(AudioFocusInfo afi) {
    int requestContext = mCarAudioContext.getContextForAttributes(
            afi.getAttributes());
    for (AudioFocusInfo holder : mFocusHolders) {
        int holderContext = mCarAudioContext.getContextForAttributes(
                holder.getAttributes());
        // 查询交互矩阵
        int interaction = sInteractionMatrix[requestContext][holderContext];
        switch (interaction) {
            case INTERACTION_CONCURRENT:
                // 允许共存，不做处理
                break;
            case INTERACTION_EXCLUSIVE:
                // 需要独占，失去焦点的Holder收到LOSS
                notifyFocusLoss(holder);
                break;
            case INTERACTION_MUTE:
                // Mute而非Loss，保持焦点但静音
                notifyFocusMute(holder);
                break;
        }
    }
}
```

### 9.8.4 CarAudioFocus三层执行机制

AAOS焦点系统存在三层执行机制，分别在不同层面确保音频行为正确：

```mermaid
flowchart TB
    subgraph layer1[第一层: 框架主动执行]
        PROP[propagateFocusLossFromGain_syncAf] --> DUCKCHECK{Loss类型判断}
        DUCKCHECK -->|LOSS_TRANSIENT_CAN_DUCK| DUCKEXEC[PlaybackActivityMonitor.duckPlayers]
        DUCKCHECK -->|LOSS| FADEEXEC[FadeOutManager.fadeOutPlayers]
    end
    subgraph layer2[第二层: App回调通知]
        PROP2[propagateFocusLossFromGain_syncAf] --> DISPATCH[FocusRequester.handleFocusLoss]
        DISPATCH --> IPC[IAudioFocusDispatcher.dispatchAudioFocusChange]
        IPC --> APPCB[App.onAudioFocusChange回调]
    end
    subgraph layer3[第三层: AAOS路径]
        PROP3[propagateFocusLossFromGain_syncAf] --> EXTPOL[mFocusPolicy外部策略]
        EXTPOL --> CZAF[CarZonesAudioFocus分发]
        CZAF --> CAF[CarAudioFocus评估]
        CAF --> ACW[AudioControlWrapper]
        ACW --> DSP[AudioControl HAL-DSP执行Ducking/Muting]
    end
```

**三层机制对比：**

| 层级 | 执行方 | 延迟 | 适用场景 | 限制 |
|------|--------|------|---------|------|
| 第一层 | AudioPolicyService | 最低(~5ms) | Duck/Fade，框架层直接操作流 | 仅限当前进程 |
| 第二层 | App进程 | 中等(~50ms) | App主动降低音量或暂停 | 依赖App合规实现 |
| 第三层 | AudioControl HAL | 可变 | AAOS专用，DSP级Ducking/Muting | 需HAL实现支持 |

### 9.8.5 设备切换全栈调用链

当蓝牙设备连接/断开时，音频路由会自动切换：

```mermaid
sequenceDiagram
    participant BT as BluetoothStack
    participant AM as AudioManager
    participant ADB as AudioDeviceBroker
    participant APS as AudioPolicyService
    participant CAS as CarAudioService
    participant CZ as CarAudioZone
    participant CVG as CarVolumeGroup
    BT->>AM: onDeviceConnected(device)
    AM->>ADB: onAudioDevicesAdded(deviceInfo)
    ADB->>APS: setDeviceConnectionState(device, CONNECTED)
    APS->>APS: 重新评估路由策略
    APS->>CAS: onAudioDevicesAdded回调
    CAS->>CZ: isAudioDeviceInfoValidForZone(device)
    CZ-->>CAS: zoneId匹配结果
    CAS->>CVG: 评估是否需要切换设备
    CVG->>CVG: 更新设备地址映射
    CAS->>CAS: setupDynamicRouting重建AudioPolicy
```

### 9.8.6 HAL Gain变化全栈调用链

当HAL层报告Gain变化（如外部放大器调整）时：

```mermaid
sequenceDiagram
    participant HAL as AudioControl HAL
    participant ACW as AudioControlWrapper
    participant CGM as CarAudioGainMonitor
    participant CZ as CarAudioZone
    participant CZC as CarAudioZoneConfig
    participant CVG as CarVolumeGroup
    participant CAS as CarAudioService
    participant App as Car App
    HAL->>ACW: onDeviceGainsChanged(reasons, gains)
    ACW->>CGM: handleAudioDeviceGainsChanged(reasons, gains)
    CGM->>CGM: convertReasonsToExtraInfo(reasons)
    Note over CGM: Reasons→EXTRA_INFO映射
    CGM->>CZ: onAudioGainChanged(reasons, gainInfos)
    CZ->>CZC: onAudioGainChanged(reasons, gainInfos)
    CZC->>CVG: onAudioGainChanged(reasons, gainInfo)
    CVG->>CVG: 根据reasons更新Gain状态机
    CVG-->>CZC: CarVolumeGroupEvent
    CZC-->>CZ: List of CarVolumeGroupEvent
    CZ-->>CAS: events
    CAS->>App: onVolumeGroupChanged回调
```

**Gain Reasons到EXTRA_INFO映射：**

| HAL Reason | CarVolumeGroupEvent EXTRA_INFO | 含义 |
|-----------|-------------------------------|------|
| REASON_MUTE | EXTRA_INFO_VOLUME_INDEX_CHANGED_BY_AUDIO_SYSTEM | 静音变化 |
| REASON_DUCK | EXTRA_INFO_VOLUME_INDEX_CHANGED_BY_AUDIO_SYSTEM | Duck变化 |
| REASON_VOLUME | EXTRA_INFO_VOLUME_INDEX_CHANGED_BY_AUDIO_SYSTEM | 音量变化 |
| REASON_THERMAL | EXTRA_INFO_VOLUME_INDEX_CHANGED_BY_AUDIO_SYSTEM | 热保护 |
| REASON_BLOCKED | EXTRA_INFO_VOLUME_INDEX_CHANGED_BY_AUDIO_SYSTEM | 被阻止 |

### 9.8.7 Ducking执行全栈调用链

当焦点变化触发Ducking时：

```mermaid
sequenceDiagram
    participant CAF as CarAudioFocus
    participant CD as CarDucking
    participant CAC as CarAudioContext
    participant ACW as AudioControlWrapper
    participant HAL as AudioControl HAL
    CAF->>CD: onFocusChange(zoneId, focusHolders, focusLosers)
    CD->>CD: updateDuckingForZoneIdLocked(zoneId)
    CD->>CD: generateNewDuckingInfoLocked(zoneId)
    CD->>CAC: evaluateAudioAttributesToDuck(holders, losers)
    CAC-->>CD: 需要Duck的AudioAttributes列表
    CD->>CD: 构建CarDuckingInfo
    CD->>ACW: setDuckingInfo(duckingInfo)
    ACW->>HAL: IAudioControl.onDevicesToDuckChange() [AIDL]
    HAL->>HAL: DSP执行Ducking
```

### 9.8.8 媒体请求全栈调用链

后排Zone请求在主Zone播放媒体音频：

```mermaid
sequenceDiagram
    participant Rear as 后排App
    participant CarAM as CarAudioManager
    participant CAS as CarAudioService
    participant MRH as MediaRequestHandler
    participant Primary as 主Zone App
    participant ACW as AudioControlWrapper
    Rear->>CarAM: requestMediaAudioOnPrimaryZone(occupantInfo, callback)
    CarAM->>CAS: requestMediaAudioOnPrimaryZone [Binder]
    CAS->>MRH: requestMediaAudioOnPrimaryZone(occupantInfo, callback)
    MRH->>MRH: 生成requestId, 创建InternalMediaAudioRequest
    MRH->>MRH: handleMediaAudioRequest(occupantInfo, requestId)
    MRH->>Primary: IPrimaryZoneMediaAudioRequestCallback.onRequestMediaOnPrimaryZone
    Note over Primary: 主Zone App收到审批请求
    Primary->>CAS: acceptMediaAudioRequest(requestId)
    CAS->>MRH: acceptMediaAudioRequest(requestId)
    MRH->>MRH: mAssignedOccupants.add(occupantInfo)
    MRH->>MRH: mRequestIdToApprover.put(requestId, callback)
    MRH->>Rear: IMediaAudioRequestStatusCallback.onMediaAudioRequestStatusChanged(APPROVED)
    CAS->>CAS: 允许后排音频路由到主Zone设备
```

### 9.8.9 跨链路交互关系

AAOS音频系统的三条核心链路并非孤立运行，存在复杂的交互关系：

```mermaid
flowchart TB
    subgraph focus_chain[焦点链路]
        FREQ[焦点请求] --> FEVAL[交互矩阵评估]
        FEVAL --> FRESULT[GRANTED/DENIED]
        FEVAL --> DUCK_EVAL[Ducking计算]
    end
    subgraph volume_chain[音量链路]
        VREQ[音量请求] --> VCHECK[Blocked状态检查]
        VCHECK --> VGAIN[Gain计算与设置]
        VGAIN --> VEVENT[CarVolumeGroupEvent]
    end
    subgraph gain_chain[HAL Gain链路]
        GHAL[HAL Gain变化] --> GCONV[Reasons转换]
        GCONV --> GSTATEM[Gain状态机更新]
        GSTATEM --> GEVENT[CarVolumeGroupEvent]
    end
    DUCK_EVAL -->|影响| VGAIN
    GSTATEM -->|Blocked/Attenuated覆盖| VGAIN
    FRESULT -->|触发| DUCK_EVAL
    VEVENT -->|通知App| FREQ
```

**交互要点：**
1. 焦点评估结果决定Ducking状态 → Ducking影响音量Gain
2. HAL Gain状态机(Blocked/OverLimit/Attenuated)覆盖音量设置
3. 音量变化事件可触发焦点重新评估
4. 三条链路通过`CarAudioService`协调统一

---